/** Marks an auth session as password-reset only (hide navbar; not a full login). */
export const PASSWORD_RECOVERY_COOKIE = "bh_password_recovery";

export const PASSWORD_RECOVERY_COOKIE_MAX_AGE_SECONDS = 60 * 60;
