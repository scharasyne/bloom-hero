import type { ReactNode } from "react";
import { VendorDashboardSidebarCard } from "@/app/(vendor)/_components/vendor-dashboard-sidebar-card";
type TabId =
  | "dashboard"
  | "products"
  | "orders"
  | "messages"
  | "profile"
  | "schedule"
  | "settings";
type VendorType = "market" | "pop-up";
type VendorDashboardShellProps = {
  activeTab: TabId;
  vendorType: VendorType;
  children: ReactNode;
  contentClassName?: string;
  sidebarClassName?: string;
};
export function VendorDashboardShell({
  activeTab,
  vendorType,
  children,
  contentClassName = "w-full p-4 lg:pl-2 lg:pr-10 md:p-6 sm:pt-20",
  sidebarClassName = "lg:p-6",
}: VendorDashboardShellProps) {
  return (
    <main className="flex min-h-screen bg-[#fbf7f4] text-[#1f1f1f]" style={{ fontFamily: "'Quicksand', sans-serif" }}>
      <div className={sidebarClassName}>
        <VendorDashboardSidebarCard activeTab={activeTab} vendorType={vendorType} />
      </div>
      <div className={contentClassName}>{children}</div>
    </main>
  );
}