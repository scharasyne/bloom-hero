/** Rebuilt modules: 10 cases each (2 Sunny, 8 Rainy). Grounded in /sign-up and GET /api/search. */

export const MODULE_RATIO_TARGETS = {
  "Implement User Registration Logic": { sunny: 2, rainy: 8 },
  "Initialize API & data flow": { sunny: 2, rainy: 8 },
};

function c(fields) {
  return {
    userStoryId: fields.userStoryId ?? "US-SP1-12",
    module: fields.module,
    testType: fields.testType ?? "Functional",
    priority: fields.priority ?? "High",
    preconditions: fields.preconditions ?? "Dev server running at localhost:3000",
    testData: fields.testData ?? "N/A",
    remarks: fields.remarks ?? "",
    route: fields.route,
    role: fields.role ?? "guest",
    ...fields,
  };
}

export const REPLACED_MODULE_CASES = [
  // --- Implement User Registration Logic (2 sunny, 8 rainy) ---
  c({
    id: "TC-SP1-075",
    module: "Implement User Registration Logic",
    userStoryId: "US-SP1-12",
    title: "Sign-up accepts valid email and matching passwords",
    caseType: "Sunny",
    testType: "UI",
    priority: "Critical",
    testData: "username: newuser01 | email: newuser01@bloomhero.test | password: TestPass123!",
    steps:
      "1. Open /sign-up\n2. Enter username, valid email (single @, valid domain)\n3. Enter matching password and confirm\n4. Click Create Account",
    expected: "Client validation passes; Supabase sign-up proceeds or shows confirm-email message",
    actual:
      "isValidEmailFormat() allows standard emails; matching passwords proceed to supabase.auth.signUp; success shows confirm message and redirects to /.",
    status: "Passed",
    route: "/sign-up",
  }),
  c({
    id: "TC-SP1-078",
    module: "Implement User Registration Logic",
    userStoryId: "US-SP1-12",
    title: "Password mismatch blocked before sign-up API",
    caseType: "Sunny",
    testType: "UI",
    priority: "High",
    testData: "email: test@customer.com | password: abc | confirm: xyz",
    steps:
      "1. Open /sign-up\n2. Enter valid email\n3. Enter different password and confirm password\n4. Click Create Account",
    expected: "Passwords do not match message; no API call",
    actual: 'handleSignUp shows "Passwords do not match." and stops before supabase.auth.signUp.',
    status: "Passed",
    route: "/sign-up",
  }),
  c({
    id: "TC-SP1-079",
    module: "Implement User Registration Logic",
    title: "Email with double @ (@@) rejected",
    caseType: "Rainy",
    testData: "email: user@@example.com",
    steps: "1. Open /sign-up\n2. Enter email user@@example.com\n3. Fill other required fields\n4. Submit",
    expected: "Invalid email error; account not created",
    actual:
      'isValidEmailFormat() rejects @@; UI shows "Enter a valid email address." without calling Supabase.',
    status: "Passed",
    route: "/sign-up",
  }),
  c({
    id: "TC-SP1-080",
    module: "Implement User Registration Logic",
    title: "Email with !@ in local part rejected",
    caseType: "Rainy",
    testData: "email: !@invalid.com",
    steps: "1. Open /sign-up\n2. Enter email !@invalid.com\n3. Submit",
    expected: "Invalid email error",
    actual:
      'Regex disallows ! in local part; message "Enter a valid email address."',
    status: "Passed",
    route: "/sign-up",
  }),
  c({
    id: "TC-SP1-081",
    module: "Implement User Registration Logic",
    title: "Email without @ symbol rejected",
    caseType: "Rainy",
    testData: "email: notanemail",
    steps: "1. Open /sign-up\n2. Enter notanemail\n3. Submit",
    expected: "Invalid email error",
    actual: "Client validation fails; HTML type=email may also block submit in browser.",
    status: "Passed",
    route: "/sign-up",
  }),
  c({
    id: "TC-SP1-082",
    module: "Implement User Registration Logic",
    title: "Empty username blocked",
    caseType: "Rainy",
    testData: "username: (empty)",
    steps: "1. Open /sign-up\n2. Leave username empty\n3. Try to submit",
    expected: "Cannot submit",
    actual: "username input has required attribute; browser blocks submit.",
    status: "Passed",
    route: "/sign-up",
  }),
  c({
    id: "TC-SP1-077",
    module: "Implement User Registration Logic",
    title: "Duplicate email shows Supabase error",
    caseType: "Rainy",
    testData: "email: test@customer.com (existing account)",
    steps: "1. Open /sign-up\n2. Use email already registered\n3. Submit valid passwords",
    expected: "Registration error; no duplicate account",
    actual: "Supabase returns error message in signup-status (e.g. User already registered).",
    status: "Passed",
    route: "/sign-up",
  }),
  c({
    id: "TC-SP1-076",
    module: "Implement User Registration Logic",
    title: "Weak password policy enforcement",
    caseType: "Rainy",
    testData: "password: 123",
    steps: "1. Open /sign-up\n2. Enter valid email and weak password\n3. Submit",
    expected: "Password strength rules enforced",
    actual:
      "No client-side strength rules in SignUp.tsx; Supabase may reject weak passwords with its own error message.",
    status: "Partial",
    route: "/sign-up",
  }),
  c({
    id: "TC-SP1-083",
    module: "Implement User Registration Logic",
    title: "Email with only whitespace rejected",
    caseType: "Rainy",
    testData: "email: (spaces only)",
    steps: "1. Open /sign-up\n2. Enter spaces in email field\n3. Submit",
    expected: "Invalid email error",
    actual: "isValidEmailFormat() rejects whitespace via trim/regex check.",
    status: "Passed",
    route: "/sign-up",
  }),
  c({
    id: "TC-SP1-084",
    module: "Implement User Registration Logic",
    title: "Email missing domain rejected",
    caseType: "Rainy",
    testData: "email: user@",
    steps: "1. Open /sign-up\n2. Enter user@\n3. Submit",
    expected: "Invalid email error",
    actual: 'Shows "Enter a valid email address." (domain segment required).',
    status: "Passed",
    route: "/sign-up",
  }),

  // --- Initialize API & data flow (2 sunny, 8 rainy) ---
  c({
    id: "TC-SP1-045",
    module: "Initialize API & data flow",
    userStoryId: "US-SP1-09",
    title: "Search API returns flowers for scope=flowers",
    caseType: "Sunny",
    testType: "API",
    priority: "High",
    testData: "GET /api/search?q=rose&scope=flowers",
    steps:
      "1. With dev server running, call GET /api/search?q=rose&scope=flowers\n2. Parse JSON body",
    expected: "HTTP 200; JSON has flowers array (may be empty if no data)",
    actual:
      "route.ts returns 200 with { flowers, vendors, errors }; flowers query runs when scope includes flowers and q is set.",
    status: "Passed",
    route: "/api/search",
  }),
  c({
    id: "TC-SP1-046",
    module: "Initialize API & data flow",
    userStoryId: "US-SP1-09",
    title: "Search API returns vendors for scope=vendors",
    caseType: "Sunny",
    testType: "API",
    priority: "High",
    testData: "GET /api/search?q=shop&scope=vendors",
    steps: "1. Call GET /api/search?q=shop&scope=vendors\n2. Inspect JSON",
    expected: "HTTP 200; vendors array present",
    actual: "fetchVendorResults runs when scope is vendors and q is non-empty; vendors key always in payload.",
    status: "Passed",
    route: "/api/search",
  }),
  c({
    id: "TC-SP1-048",
    module: "Initialize API & data flow",
    title: "Empty query and no category returns empty arrays",
    caseType: "Rainy",
    testData: "GET /api/search?q=&scope=all",
    steps: "1. Call GET /api/search with empty q and no category\n2. Read response",
    expected: "HTTP 200; flowers: []; vendors: []",
    actual: "Early return in route.ts when !hasQuery && !hasCategory returns { flowers: [], vendors: [] }.",
    status: "Passed",
    route: "/api/search",
  }),
  c({
    id: "TC-SP1-049",
    module: "Initialize API & data flow",
    title: "Invalid price parameter ignored",
    caseType: "Rainy",
    testData: "GET /api/search?q=rose&scope=flowers&price=not_a_price",
    steps: "1. Call API with invalid price\n2. Compare to price=Any",
    expected: "No crash; results same as Any when price not recognized",
    actual: "Only <500, 500-700, >700 change filter; other values treated as Any.",
    status: "Passed",
    route: "/api/search",
  }),
  c({
    id: "TC-SP1-051",
    module: "Initialize API & data flow",
    title: "Unknown scope falls back to all",
    caseType: "Rainy",
    testData: "GET /api/search?q=flower&scope=invalid_scope",
    steps: "1. Call with scope=invalid_scope\n2. Inspect flowers and vendors keys",
    expected: "Both flowers and vendors may return when q is set",
    actual: "normalizeSearchScope() maps unknown values; with q set, both flower and vendor fetches can run for scope=all.",
    status: "Passed",
    route: "/api/search",
  }),
  c({
    id: "TC-SP1-047",
    module: "Initialize API & data flow",
    title: "scope=all with query returns both arrays",
    caseType: "Rainy",
    testData: "GET /api/search?q=bloom&scope=all",
    steps: "1. Call search with scope=all and q=bloom\n2. Verify JSON shape",
    expected: "flowers and vendors arrays; status 200 or 207 if partial error",
    actual: "Parallel fetchFlowerResults and fetchVendorResults; errors array populated on partial failure (207).",
    status: "Passed",
    route: "/api/search",
  }),
  c({
    id: "TC-SP1-052",
    module: "Initialize API & data flow",
    title: "Price sort applies to flowers only",
    caseType: "Rainy",
    testData: "GET /api/search?q=rose&scope=all&sort=Price: Low to High",
    steps: "1. Call with sort=Price: Low to High\n2. Check flower price ordering",
    expected: "Flowers sorted by price ascending; vendors unaffected",
    actual: "fetchFlowerResults orders by price when sort matches; vendor sort uses shop_name unless Best Sellers.",
    status: "Passed",
    route: "/api/search",
  }),
  c({
    id: "TC-SP1-050",
    module: "Initialize API & data flow",
    title: "Valid category filter with query",
    caseType: "Rainy",
    testData: "GET /api/search?q=&scope=flowers&category=birthday",
    steps: "1. Call with category=birthday (no q)\n2. Inspect flowers array",
    expected: "HTTP 200; flowers filtered to category or empty if none",
    actual: "hasCategory allows request; product_categories filter applied in fetchFlowerResults.",
    status: "Passed",
    route: "/api/search",
  }),
  c({
    id: "TC-SP1-045b",
    module: "Initialize API & data flow",
    title: "Best Sellers sort without query uses category or empty",
    caseType: "Rainy",
    testData: "GET /api/search?scope=flowers&sort=Best Sellers",
    steps: "1. Call without q, only sort and scope\n2. Read response",
    expected: "Empty arrays (q required unless category)",
    actual: "Without q or category, returns empty flowers/vendors arrays per route guard.",
    status: "Passed",
    route: "/api/search",
  }),
  c({
    id: "TC-SP1-046b",
    module: "Initialize API & data flow",
    title: "Special characters in query are URL-encoded",
    caseType: "Rainy",
    testData: "GET /api/search?q=rose%20bouquet&scope=flowers",
    steps: "1. Call API with encoded space in q\n2. Confirm 200 response",
    expected: "No server error; search runs",
    actual: "q trimmed from searchParams; ilike pattern search on product_name/description.",
    status: "Passed",
    route: "/api/search",
  }),
];

export const REPLACED_MODULE_NAMES = new Set(
  Object.keys(MODULE_RATIO_TARGETS)
);
